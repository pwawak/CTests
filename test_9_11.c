//
// Created by Piotr on 26-Dec-18.
//

/*
 * Napisz funkcje do konwersji liczb na ciąg znaków, funkcje powinny zwracać 0 w przypadku,
 * kiedy konwersja się powiodła lub 1 w przeciwnym przypadku.
 * Prototypy funkcji powinny wyglądać następująco:
 *		int my_itoa(int a, char tab[], int size);
 *		int my_ftoa(float a, int precision, char tab[], int size);
 *		Napisz program, który pobierze od użytkownika liczbę zmiennoprzecinkową.
 *		Następnie korzystając z powyższych funkcji dokona konwersji wprowadzonej liczby do ciągu znaków.
 *		Jako parametr funkcji itoa ma zostać podana część całkowita podanej liczby, a w przypadku konwersji
 *		z float na ciąg znaków, wymagana jest dokładność 4 miejsc po przecinku. Następnie ma zostać wyświetlony
 *		w oddzielnych liniach wynik konwersji, najpierw z typu int, a następnie z typu float.
 *		W przypadku podania błędnych danych program powinien wyświetlić komunikat Incorrect input
 *		i zakończyć działanie z kodem błędu 1.
 *		Uwaga: W programie nie można korzystać z bibliotek stdlib i string oraz z funkcji z rodziny scanf
 *		(oprócz funkcji scanf).
 */

#include <stdio.h>
#include <ctype.h>

int my_itoa(int a, char tab[], int size) {

}

// -------------------------------------------------------------------------------------

int my_ftoa(float a, int precision, char tab[], int size) {

}

// -------------------------------------------------------------------------------------

int main() {

}